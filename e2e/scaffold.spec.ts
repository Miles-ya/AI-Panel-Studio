import { test } from "@playwright/test";

test.describe.skip("Stage 1 scaffold", () => {
  test("reserves the root e2e directory for later MVP flows", async () => {
    // Product flows are intentionally outside the Stage 1 scope.
  });
});
